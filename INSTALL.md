# 🦅 Jarvis Installation Guide

Ein modulares Setup für **Linux (Kali/Mint/Ubuntu)** und **Windows**.

---

## 🐧 Linux Installation

### 1. Voraussetzungen
```bash
sudo apt update && sudo apt install git zsh tilix guake fonts-firacode -y
