# 🦅 Jarvis Dotfiles
Automated System Configuration.

## 🛠 Verfügbare Befehle
| Befehl | Beschreibung |
|---|---|
| **`g`** | Git-Aliase (g=git, gs=status, ga=add, gc=commit, etc.) |
| **`logusr`** | Zeigt "echte" User (UID >= 1000) und Root an. |
| **`sysup`** | Führt ein volles System-Update durch. |
| **`myip`** | Zeigt die öffentliche IP-Adresse an. |
| **`mkcd`** | Erstellt Verzeichnis und wechselt hinein. |
| **`www`** | Startet einen Webserver auf Port 8000. |
| **`ff`** | Findet Dateien im aktuellen Ordner (Fuzzy-Search). |
| **`ports`** | Zeigt offene Ports und lauschende Dienste. |
| **`extract`** | Universal-Entpacker für alle Formate. |
| **`conf`** | Config-Editor mit Auto-Reload. |
| **`dotsync`** | Sync mit GitHub + Auto-Doku + Reload. |
| **`[-f~/.fzf.zsh]&&source~/.fzf.zsh`** | Fuzzy Finder Integration. |
